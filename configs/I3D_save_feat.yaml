action: save 
name: ??? #local: saved_feat_I3D
split: ??? #local:train
resume_from: ??? #local:./saved_models/I3D_SourceOnlyD1
#from default.yaml
gpus: ??? # gpus adopted
wandb_name: null # needed for wandb logging
logname: null # name of the logs
models_dir: null # directory containing all the models
modality: ["RGB"] # modality used
total_batch: 128 # total batch size if training is done with gradient accumulation
batch_size: 32 # batch size for the forward


save:
  num_clips: ??? #local: 5
  dense_sampling:
    RGB: ??? #local: True
  num_frames_per_clip:
    RGB: 16

dataset:
  annotations_path: train_val
  shift: ??? #local:D1-D1
  workers: 4
  stride: 2
  resolution: 224
  RGB:
    data_path: ??? #local: data/epicKitchenDA/rgb_flow/P08_unzipped
    tmpl: "img_{:010d}.jpg"
  Event:
    rgb4e: 6

# these are the action recognition models for each modality
models:
  RGB:
    model: I3D
    dropout: 0.5
    normalize: False
    resolution: 224
    kwargs: {}
    lr_steps: 3000
    lr: 0.01
    sgd_momentum: 0.9
    weight_decay: 1e-7
    weight_i3d_rgb: './pretrained_i3d/rgb_imagenet.pt'


